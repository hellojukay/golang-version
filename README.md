# golang-version
a demo for golang to show build version
# build
```shell
chmod +x build.sh
./build.sh
```

# run 
```shell
hellojukay@local golang-version (master) $ ./golang-version -v
master-03cf576
```
![img](./demo.gif)